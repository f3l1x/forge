# Configurator Panel [deprecated]

Forerunner of Nette\DI\Diagnostics\ContainerPanel.

## Usage
    // Create container
    $container = $configurator->createContainer();

    // Register panel
    \Panel\Configurator::register($container);

That's all!